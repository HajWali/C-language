#include <stdio.h>

int main ()
{
    int preNum, currNum;
    
    

    for(int i =1; i<5; i++)
    {
        printf("Enter the %d Number: ", i);
        scanf("%d", &Num);
        if(i==1)
          preNum = num;
        else
          {
            if(num == preNum)
            num++;
          }

          printf("")
}